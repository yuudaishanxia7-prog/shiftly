import {hash} from "bcryptjs";import {prisma} from "@/lib/prisma";import {apiError,requireManager} from "@/lib/access";import {staffSchema} from "@/lib/validation";
export async function GET(){try{const u=await requireManager();return Response.json(await prisma.user.findMany({where:{storeId:u.storeId},select:{id:true,name:true,email:true,role:true,isActive:true,createdAt:true},orderBy:{name:"asc"}}))}catch(e){return apiError(e)}}
export async function POST(req:Request){try{const u=await requireManager();const body=staffSchema.parse(await req.json());const created=await prisma.user.create({data:{storeId:u.storeId,name:body.name,email:body.email,passwordHash:await hash(body.password,12),role:body.role},select:{id:true,name:true,email:true,role:true,isActive:true}});return Response.json(created,{status:201})}catch(e){return apiError(e)}}
