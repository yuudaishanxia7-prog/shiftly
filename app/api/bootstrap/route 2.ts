import {prisma} from "@/lib/prisma";import {apiError,requireUser} from "@/lib/access";
export async function GET(){try{const me=await requireUser();const store=await prisma.store.findUniqueOrThrow({where:{id:me.storeId},include:{settings:true}});const periods=await prisma.shiftPeriod.findMany({where:{storeId:me.storeId},orderBy:[{year:"desc"},{month:"desc"}]});const period=periods[0]??null;if(!period)return Response.json({me,store,periods,period:null,requests:[],submissions:[],confirmed:[],users:[]});const manager=me.role==="manager";const [requests,submissions,confirmed,users]=await Promise.all([prisma.shiftRequest.findMany({where:{shiftPeriodId:period.id,...(manager?{user:{storeId:me.storeId}}:{userId:me.id})},include:{user:{select:{id:true,name:true}}}}),manager?prisma.shiftSubmission.findMany({where:{shiftPeriodId:period.id,user:{storeId:me.storeId}},include:{user:{select:{id:true,name:true}}}}):Promise.resolve([]),prisma.confirmedShift.findMany({where:{shiftPeriodId:period.id,user:{storeId:me.storeId},...(manager||period.status==="published"?{}:{userId:me.id})},include:{user:{select:{id:true,name:true}}}}),manager?prisma.user.findMany({where:{storeId:me.storeId},select:{id:true,name:true,email:true,role:true,isActive:true},orderBy:{name:"asc"}}):period.status==="published"?prisma.user.findMany({where:{storeId:me.storeId,isActive:true},select:{id:true,name:true,email:true,role:true,isActive:true},orderBy:{name:"asc"}}):Promise.resolve([])]);return Response.json({me,store,periods,period,requests,submissions,confirmed,users})}catch(e){return apiError(e)}}
